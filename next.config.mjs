/** @type {import('next').NextConfig} */
export default {
  // Railway runs `next start`; nothing Vercel-specific here.

  // The floating Next.js dev badge. It never shipped to production anyway —
  // this just keeps it out of the way while working.
  devIndicators: false,
};
